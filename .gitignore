# Dependencies
node_modules/

# Build output
dist/

# Secrets — NEVER commit
.dev.vars
.env
.env.*
*.local

# Cloudflare Workers local state
.wrangler/

# Test artifacts
test-results/
playwright-report/
blob-report/
.last-run.json

# OS / editor
.DS_Store
*.log

# Temporary: token lacks `workflow` scope, so the CI workflow can't be pushed
# yet. Remove this line after `gh auth refresh -s workflow`, then commit the file.
.github/workflows/ci.yml
